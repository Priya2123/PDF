import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  text: {
    // color: "black",
    textDecoration: "none",
  },
  icon: {
    // color: "black",
    cursor: "pointer",
    margin: "0 2%",
  },
}));
