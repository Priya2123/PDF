import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  text: {
    color: "#fff",
    textDecoration: "none",
  },
  icon: {
    color: "#fff",
    cursor: "pointer",
    margin: "0 2%",
  },
}));
